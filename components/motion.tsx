'use client';
import {useEffect} from 'react';
export default function Motion(){useEffect(()=>{const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;if(reduce)return;const els=document.querySelectorAll('.reveal');const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.08});els.forEach(e=>{e.classList.add('will-reveal');observer.observe(e)});let ticking=false;const update=()=>{const y=scrollY;document.querySelectorAll<HTMLElement>('.hero-image').forEach(el=>{el.style.transform=`translateY(${Math.min(y*.16,180)}px) scale(1.04)`});const progress=document.querySelector<HTMLElement>('.reading-progress');if(progress)progress.style.transform=`scaleX(${y/Math.max(1,document.documentElement.scrollHeight-innerHeight)})`;ticking=false};const onScroll=()=>{if(!ticking){requestAnimationFrame(update);ticking=true}};addEventListener('scroll',onScroll,{passive:true});return()=>{observer.disconnect();removeEventListener('scroll',onScroll)}},[]);return <div aria-hidden="true" className="reading-progress"/>}
