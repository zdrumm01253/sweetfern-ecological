'use client';
import {watermarkedPlan} from "@/lib/plan-watermarks";

import {useState} from 'react';import {Tabs,TabsList,TabsTrigger,TabsContent} from '@/components/ui/tabs';import {approach} from '@/lib/services';
export default function ApproachExplorer(){const[step,setStep]=useState('0');return <Tabs value={step} onValueChange={v=>setStep(String(v))} className="approach-explorer"><TabsList aria-label="Sweetfern process" className="approach-list">{approach.map((s,i)=><TabsTrigger key={s.title} value={String(i)}><span>0{i+1}</span>{s.title}<b>↗</b></TabsTrigger>)}</TabsList><div>{approach.map((s,i)=><TabsContent value={String(i)} key={s.title} className="approach-panel"><img src={watermarkedPlan(s.title==='Adapt'?'/images/meadow-reclamation/meadow-and-hills-updated.webp':`/images/summer-${s.image}.webp`)} alt={s.title==='Adapt'?'Flowering meadow with paths and wooded hills':s.title+' — landscape context'}/><div><p className="eyebrow">0{i+1} / {s.title}</p><h2>{s.summary}</h2><p>{s.detail}</p><button onClick={()=>setStep(String((i+1)%approach.length))}>{i===7?'Return to Survey':'Next: '+approach[i+1].title} →</button></div></TabsContent>)}</div></Tabs>}
